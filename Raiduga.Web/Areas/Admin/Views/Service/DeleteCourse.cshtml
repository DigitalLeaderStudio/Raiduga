@model Raiduga.Web.Models.Service.CourseViewModel
@using Raiduga.Web.Localization;

@{
	var title = Model.Name + " " + Translations.Delete;
	ViewBag.Title = title;
	Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>@title</h2>

<div class="panel panel-default">
	<div class="panel-heading">
		<h3>@title</h3>
	</div>
	<div class="panel-body">
		<p>
			@Translations.Course_DeletePrompt
		</p>
	</div>
	<div class="panel-footer">
		@using (Html.BeginForm())
		{
			@Html.AntiForgeryToken()
			@Html.HiddenFor(m => m.Id)

			<a href="@Url.Action("Edit", "Service", new { id = Model.ServiceId })" class="btn btn-default">@Translations.Cancel</a>
			<input type="submit" class="btn btn-danger" value="@Translations.Delete" />
		}
	</div>
</div>